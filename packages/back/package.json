{
  "name": "@oboku/plugin-imhentai-back",
  "private": false,
  "version": "0.0.1",
  "scripts": {
    "build": "tsc",
    "prepublishOnly": "npm run build"
  },
  "devDependencies": {
    "@oboku/plugin-back": "^0.0.4"
  }
}
